DATABASE_URL="file:./dev.db"

# Optional. When set, WhereTo automatically switches from its built-in mock
# Bangalore venue/travel-time data to live Google Maps Platform data
# (Places, Distance Matrix, Geocoding). Leave unset to run entirely offline
# with realistic mock data — no key required to develop or demo the app.
# GOOGLE_MAPS_API_KEY=
